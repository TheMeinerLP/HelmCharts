apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bluemap.fullname" . }}-test-connection"
  labels:
    {{- include "bluemap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bluemap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
