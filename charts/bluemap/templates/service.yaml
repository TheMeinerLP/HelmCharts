apiVersion: v1
kind: Service
metadata:
  name: {{ include "bluemap.fullname" . }}
  labels:
    {{- include "bluemap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bluemap.selectorLabels" . | nindent 4 }}
